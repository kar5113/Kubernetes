apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  namespace: roboshop-dev
spec:
  containers:
  - name: nginx-container
    image: nginx
    ports: 
    - containerPort: 80
  # namespace: roboshop-dev